<Window x:Class="SearchEngine.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:SearchEngine"
        mc:Ignorable="d"
        Title="Вход в систему" Height="250" ResizeMode="NoResize" Width="400" Icon="/Resources/Logo.ico">
    <Grid>
        <StackPanel>
            <TextBlock Text="Вход в поисковую систему 3D-сканнеров"
                       Margin="10,10,10,10"
                       FontSize="18"/>
            <TextBox x:Name="UserNameTextBox"
                     Margin="10,10,10,10"
                     FontSize="18"
                     Text="Имя пользователя" 
                     GotFocus="UserNameTextBoxGotFocus" 
                     LostFocus="UserNameTextBoxLostFocus"
                     HorizontalAlignment="Left"
                     Width="200"/>
            <TextBox x:Name="PasswordTextBox"
                     Margin="10,10,10,10"
                     FontSize="18"
                     Text="Пароль" 
                     GotFocus="PasswordTextBoxGotFocus" 
                     LostFocus="PasswordTextBoxLostFocus"
                     HorizontalAlignment="Left"
                     Width="200"/>
            <Button Name="LoginButton"
                    Margin="10,10,10,10"
                    Content="Войти"
                    HorizontalAlignment="Center"
                    Width="150"
                    Click="LoginButtonClick"/>
        </StackPanel>
    </Grid>
</Window>
